import QtQuick
import QtQuick.Layouts
import QtQuick.Controls
import Buttons
import Panels
import ConfigComponent
import Base

Item {
    id: root
    anchors.fill: parent
    anchors.leftMargin: 6

    ColumnLayout {
        id: panel
        anchors.fill: root
    }
}

